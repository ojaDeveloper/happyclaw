# HappyClaw 待实现需求

## 1. 支持 member 用户使用 host 模式

**背景**：当前只有 admin（folder=main）可以在宿主机运行，member 强制 Docker 容器模式。在无 Docker 的 Windows 环境下，member 用户无法使用。

**场景**：用户想创建两个 Telegram bot（一个用 DeepSeek，一个用 Claude），各绑定独立工作区，各用不同模型配置，都在宿主机运行。

**现状限制**：
- `ensureUserHomeGroup()`（db.ts:2414）新建时 member 强制 `container`
- `loadState()`（index.ts:1880）启动时 admin → host，member → container
- 多个 admin 共享同一个 `web:main`，无法隔离

**改动范围**：
- `src/db.ts` — `ensureUserHomeGroup()` 允许 member 使用 host
- `src/index.ts` — `loadState()` 不再强制覆盖 executionMode
- 可能需要 Web 设置页增加执行模式选择

**风险**：member 在 host 模式下拥有宿主机文件系统完整访问权限（bypassPermissions），安全隔离失效。适合自用场景，不适合多租户。

## 2. 自定义客户端（借助 IM 通道中转）

**背景**：飞书/Telegram 的原生聊天界面无法在发消息前设置初始化参数（选模型、选工作区、预设 prompt 模板、附加上下文等）。同时 HappyClaw 部署在内网没有公网 IP，无法直接用 WebSocket 从外部连接。

**核心思路**：利用飞书/Telegram 作为免费的内网穿透通道。客户端通过 IM 平台的 OpenAPI 发消息，HappyClaw 通过已有的 IM 长连接收消息并回复，客户端再通过 IM API 拉取回复。

**架构**：
```
自定义客户端 App
├── 参数设置面板（模型、工作区、prompt 模板等）
├── 聊天界面（发消息、收回复）
├── 飞书通道 → 飞书 OpenAPI 发消息 / 事件订阅收回复
└── Telegram 通道 → Bot API 发消息 / getUpdates 拉回复
```

**优势**：
- 客户端和 HappyClaw 都不需要公网 IP
- 可以在 IM 原生界面基础上增加自定义 UI（参数、模板、上下文初始化）
- 支持手机/桌面多端

**备注**：这是独立客户端项目，不需要改 HappyClaw 代码。

## 3. 群组级"轻量模式"——跳过系统提示词注入

**背景**：当前每轮对话都会注入大量系统提示词（用户 profile、安全守则、记忆系统指南、渠道格式、HEARTBEAT 等），导致每轮缓存写入约 6 万 tokens、费用 $0.24+。对于简单对话或纯执行任务场景，这些上下文完全多余。

**场景**：
- 只想让 Agent 当一个简单聊天机器人
- 只想执行一个 bash 命令或文件操作
- 使用国产模型（上下文窗口小，如 DeepSeek V3.1 只有 128k），多余的上下文浪费宝贵窗口

**方案**：在群组设置中增加"提示词模式"选项，通过 IPC 传递给 agent-runner：
- **完整模式**（默认）：注入所有 L0-L4 层系统提示词，适合主工作区
- **轻量模式**：只保留最基本的行为约束（L2 的简化版），跳过 profile、记忆、HEARTBEAT、渠道格式等
- **无注入模式**：完全不追加系统提示词，只用 Claude Code 内置 preset

**改动范围**：
- `container/agent-runner/src/index.ts` — `systemPromptAppend` 构建逻辑根据模式裁剪
- `container/agent-runner/src/types.ts` — `ContainerInput` 增加 `promptMode` 字段
- `src/index.ts` — 传递群组的 `promptMode` 给 agent-runner
- `src/db.ts` — `registered_groups` 表增加 `prompt_mode` 列
- Web 前端群组设置页增加选项

## 4. 多 Agent 协作系统

**背景**：当前所有 IM 渠道共享同一个用户的同一个工作区和 Agent。希望不同的 IM bot 是独立个体（各自有独立工作区、模型配置、人格），并且 Agent 之间可以交流、委派任务、协作完成复杂工作。

**场景**：
- 飞书 Bot 1（DeepSeek）负责日常编程，飞书 Bot 2（Claude）负责复杂推理
- Agent A 遇到不擅长的任务，自动委派给 Agent B
- 两个 Agent 讨论方案后合并结果

**分阶段实现**：

### 阶段一（近期）：独立个体
依赖需求 1（member host 模式）。每个 bot 对应一个 HappyClaw 用户，各有独立的主容器、工作区、模型配置。彼此完全隔离，不交流。

### 阶段二（中期）：单向任务委派
新增 MCP 工具 `delegate_task`，Agent 可以把任务委派给另一个 Agent：

```
Agent A 调用 delegate_task(target='Agent B', task='审查这段代码', context={files, description})
  → 主进程路由：向 Agent B 的 IPC 通道注入消息
  → Agent B 执行完成后通过 send_message 回传结果
  → 主进程将结果注入回 Agent A 的 IPC 通道
```

**改动范围**：
- `container/agent-runner/src/mcp-tools.ts` — 新增 `delegate_task` 工具定义
- `src/index.ts` — IPC watcher 增加 `delegate_task` 类型处理，跨工作区消息路由
- 需要设计防死循环机制（委派深度限制、禁止回环）

### 阶段三（远期）：双向协作
- **消息总线**：Agent 之间通过主进程中转的消息通道实时对话
- **共享工作区**：通过 mount-allowlist 让多个工作区挂载同一个目录，实现文件级协作
- **协调者模式**：可选的"协调 Agent"负责分配任务和汇总结果

**核心难点**：
- 上下文传递：委派时怎么传足够的背景，不丢关键信息
- 结果同步：Agent B 改了文件，Agent A 怎么知道改了什么
- 并发冲突：两个 Agent 同时改同一个文件
- 死循环防护：A 委派给 B，B 又委派回 A

## 5. 重新评估多 Provider 轮询策略

**背景**：当前支持 round-robin / weighted-round-robin / failover 三种 Provider 选择策略。但 Agent 进程启动后整个会话期间不会换 Provider，轮询只在每次启动新进程时生效。

**问题**：round-robin 在实际使用中意义不大——不同模型能力、上下文格式不同，轮换导致体验不一致，用户无法控制本次用哪个模型。

**建议**：
- 考虑只保留 `failover`（主备模式）：主 Provider 挂了自动切备用，体验一致
- 或者改为用户可选：每次发消息时手动选 Provider（类似之前的模型选择器，但切的是 Provider 而非 model 名）
- 当前 Web 设置页未暴露 `balancing.strategy` 配置项，如果保留多策略需要加 UI
