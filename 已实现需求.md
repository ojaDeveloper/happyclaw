# HappyClaw 已实现需求

## 1. 纯净模式（Pure Mode）

**实现日期**：2026-04-07

**需求来源**：待实现需求 #3（群组级"轻量模式"——跳过系统提示词注入）

**功能说明**：

通过 `/pure` 斜杠命令切换纯净模式。开启后：

- 跳过所有自定义系统提示词注入（CLAUDE.md、安全规则、交互原则、记忆系统、心跳摘要、渠道格式、输出指南等 L0-L4 全部层级）
- 仅保留 Claude Code SDK 内置 preset
- 从全局记忆目录的 `纯净模式.md` 文件读取自定义提示词注入（用户可在记忆管理页面随时编辑）
- 消息上下文限制为最近 10 条（cursor 仍正确推进，不会重复处理）

**使用方式**：

| 命令 | 效果 |
|------|------|
| `/pure` | 切换纯净模式（无参数 toggle） |
| `/pure on` | 开启纯净模式 |
| `/pure off` | 关闭纯净模式 |

- 支持所有 IM 渠道（飞书 / Telegram / QQ / 钉钉）和 Web 端
- 状态持久化到数据库，重启后保持
- 首次开启时自动创建 `data/groups/user-global/{userId}/纯净模式.md` 默认文件

**涉及文件**：

| 文件 | 改动 |
|------|------|
| `src/types.ts` | `RegisteredGroup` 添加 `pure_mode` 字段 |
| `src/db.ts` | 数据库迁移 v35→v36，`registered_groups` 表增加 `pure_mode` 列 |
| `src/container-runner.ts` | `ContainerInput` 添加 `pureMode` 字段 |
| `container/agent-runner/src/types.ts` | `ContainerInput` 添加 `pureMode` 字段 |
| `src/index.ts` | `/pure` 命令处理、消息限 10 条、传递 pureMode 到 agent-runner |
| `src/web.ts` | Web 端 `/pure` 命令处理 |
| `container/agent-runner/src/index.ts` | 纯净模式下从 `纯净模式.md` 读取提示词替换 systemPromptAppend |
